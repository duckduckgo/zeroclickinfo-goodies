{
    "id": "salt_api_cheat_sheet",
    "name": "Salt API",
    "description": "Common commands for SaltStack API",
    "metadata": {
        "sourceName": "SaltStack",
        "sourceUrl": "https://docs.saltstack.com/en/latest/ref/cli/"
    },
    "template_type": "terminal",
    "section_order": [
        "Options",
        "Logging Options"
    ],
    "sections": {
        "Options": [{
            "val": "Print the version of Salt that is running",
            "key": "salt --version"
        }, {
            "val": "Show program's dependencies and version number, and then exit",
            "key": "salt --versions-report"
        }, {
            "val": "Show the help message and exit",
            "key": "salt -h"
        }, {
            "val": "The location of the Salt configuration directory. This directory contains the configuration files for Salt master and minions. The default location on most systems is /etc/salt",
            "key": "salt -c"
        }, {
            "val": "Specify user to run salt-minion",
            "key": "salt -u"
        }, {
            "val": "Run salt-minion as a daemon",
            "key": "salt -d"
        }, {
            "val": "Specify the location of the pidfile. Default: /var/run/salt-minion.pid",
            "key": "salt --pid-file"
        }],
        "Logging Options": [{
            "val": "Console logging log level. One of all, garbage, trace, debug, info, warning, error, quiet. Default: warning",
            "key": "salt-api -l"
        }, {
            "val": "Log file path. Default: /var/log/salt/api",
            "key": "salt-api --log-file"
        }, {
            "val": "Logfile logging log level. One of all, garbage, trace, debug, info, warning, error, quiet. Default: warning",
            "key": "salt-api --log-file-level"
        }]
    }
}
