{
    "id": "salt_cheat_sheet",
    "name": "Salt",
    "description": "Common commands for SaltStack Master/Minion",
    "metadata": {
        "sourceName": "SaltStack Website",
        "sourceUrl": "https://docs.saltstack.com/en/latest/ref/cli/"
    },
    "template_type": "terminal",
    "section_order": [
        "Format",
        "salt-key Commands",
        "salt-minion/master Commands",
        "Other helpful Commands"
    ],
    "sections": {
        "Format": [{
            "val": "The instruction is case-sensitive.",
            "key": "INSTRUCTION arguments"
        }, {
            "val": "Comments start with a #",
            "key": "# Comment"
        }],
        "salt-key Commands": [{
            "val": "List all keys",
            "key": "salt-key -l"
        }, {
            "val": "Accept the specified public key (use --include-all to match rejected keys in addition to pending keys). Globs are supported",
            "key": "salt-key -a <minion_name> "
        }, {
            "val": "Reject the specified public key (use --include-all to match accepted keys in addition to pending keys). Globs are supported.",
            "key": "salt-key -r"
        }, {
            "val": "Rejects all pending keys.",
            "key": "salt-key -A"
        }, {
            "val": "salt-key -d  <minion_name>",
            "key": "Delete the specified key. Globs are supported."
        }, {
            "val": "Delete all keys.",
            "key": "salt-key -D"
        }],
        "salt-minion/master Commands": [{
            "val": "Print the version of Salt that is running.",
            "key": "salt --version"
        }, {
            "val": "Show program's dependencies and version number, and then exit",
            "key": "salt --versions-report"
        }, {
            "val": "Show the help message and exit",
            "key": "salt -h"
        }, {
            "val": "The location of the Salt configuration directory. This directory contains the configuration files for Salt master and minions. The default location on most systems is /etc/salt.",
            "key": "salt -c"
        }, {
            "val": "Specify user to run salt-minion",
            "key": "salt -u"
        }, {
            "val": "Run salt-minion as a daemon",
            "key": "salt -d"
        }, {
            "val": "Specify the location of the pidfile. Default: /var/run/salt-minion.pid",
            "key": "salt --pid-file"
        }],
        "Other helpful Commands": [{
            "val": "After setting up the master it's a good idea to tap the even bus to watch what's going on around the network.",
            "key": "salt-run state.event pretty=True"
        }]
    }
}
