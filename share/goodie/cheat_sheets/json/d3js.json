{
    "id": "d3js_cheat_sheet",
    "name": "D3js",

    "description": "Data Driven Document (d3js) syntax",
    "metadata": {
        "sourceName": "D3js",
        "sourceUrl": "http://d3js.org/"
    },
    "aliases": [
        "d3", "datadrivendocument", "data-driven document", "data driven document", "d3 js"
    ],
    "template_type": "code",
    "section_order": ["Selections", "Data", "Transitions", "Scales", "Axes", "Interactivity", "Other Useful JavaScript"],
    "sections": {
        "Axes": [{
            "val": "Creates a new axis generator function",
            "key": "d3.svg.axis()"
        }, {
            "val": "Specifies the scale to be used with this axis",
            "key": "axis.scale()"
        }, {
            "val": "Specifies the orientation for this axis",
            "key": "axis.orient()"
        }, {
            "val": "Suggests a number of ticks for this axis",
            "key": "axis.ticks()"
        }, {
            "val": "Calls a method; used to generate an axis",
            "key": "selection.call()"
        }],
        "Data": [{
            "val": "Binds an array of data values to the selection",
            "key": "selection.data()"
        }, {
            "val": "Returns a selection of new placeholder elements",
            "key": "selection.enter()"
        }],
        "Interactivity": [{
            "val": "Binds an event listener to a selection within an anonymous function",
            "key": "selection.on()"
        }],
        "Other Useful JavaScript": [{
            "val": "Returns a random value between 0.0 and 1.0",
            "key": "Math.random()"
        }, {
            "val": "Rounds down to the nearest integer",
            "key": "Math.floor()"
        }, {
            "val": "Appends a new value to an existing array",
            "key": "array.push()"
        }],
        "Scales": [{
            "val": "Creates a new linear scale function",
            "key": "d3.scale.linear()"
        }, {
            "val": "Sets the scale’s input domain",
            "key": "scale.domain()"
        }, {
            "val": "Sets the scale’s output range",
            "key": "scale.range()"
        }, {
            "val": "Returns the smallest value in an array",
            "key": "d3.min()"
        }, {
            "val": "Returns the largest value in an array d",
            "key": "d3.max()"
        }],
        "Selections": [{
            "val": "Returns the element found",
            "key": "d3.select()"
        }, {
            "val": "Returns all found elements",
            "key": "d3.selectAll()"
        }, {
            "val": "Creates a new element inside the selection",
            "key": "selection.append()"
        }, {
            "val": "Removes the selection from the DOM",
            "key": "selection.remove()"
        }, {
            "val": "Sets the text content of the selection",
            "key": "selection.text()"
        }, {
            "val": "Set an HTML attribute value on the selection",
            "key": "selection.attr()"
        }, {
            "val": "Set an inline CSS style on the selection",
            "key": "selection.style()"
        }, {
            "val": "Adds or removes a class from the selection",
            "key": "selection.classed()"
        }],
        "Transitions": [{
            "val": "Initiates a new transition",
            "key": "selection.transition()"
        }, {
            "val": "Sets the transition duration, in milliseconds",
            "key": "selection.duration()"
        }, {
            "val": "Sets the transition delay, in milliseconds",
            "key": "transition.delay()"
        }]
    }
}
