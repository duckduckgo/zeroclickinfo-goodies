DDH.calendar_today = DDH.calendar_today || {}; // create the namespace in case it doesn't exist


DDH.calendar_today.build = function(ops) {
    Spice.registerHelper('calendarTable', function(days, start_day, prev_month_days, next_month_days, options) { 
        while (dayIndex < days.length -1 ){
		var dayIndex = 0; 
                dayIndex = dayIndex + 1;
        }
    });
    return {
        // Specify any frontend display properties here
    };
    
};
